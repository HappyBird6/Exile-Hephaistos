package com.poe2craft.crafting.domain.probability;

/*
 * crafting/domain/probability: 확률 값과 상태 전이 분포의 수치 연산을 둔다.
 * Probability: 유효 범위와 정밀도를 보장하는 확률 값의 역할을 맡는다.
 * rational 또는 BigDecimal 기반으로 계산하고 overflow를 방지한다. 표시용 반올림은 분리한다.
 */
